OCT_contracts = {

	# ==================================================
	# 前提:
	# - NFで以下のフラグ/条件を獲得済み
	#   OCT_can_offer_uk_deal
	#   OCT_can_offer_new_world_deals
	# ==================================================

	#--------------------------
	# 対英国: Mostro–London 金融合弁
	#--------------------------
	OCT_offer_uk_financial_deal = {
		icon = GFX_goal_generic_positive_trade_relations
		allowed = {
			original_tag = OCT
			has_country_flag = OCT_can_offer_uk_deal
			ENG = { exists = yes }
		}
		visible = {
			ENG = { exists = yes }
		}
		available = {
			is_subject = no
			NOT = { has_war_with = ENG }
			ENG = {
				is_subject = no
				NOT = { has_war_with = OCT }
			}
		}
		cost = 25
		fire_only_once = yes

		complete_effect = {
			ENG = { country_event = { id = OCT_uk_deal.1 days = 1 } }
		}
	}

	#--------------------------
	# 対新大陸: Mostro 投資オファー（汎用）
	# 対象例: USA / CAN / BRA / MEX など
	#--------------------------

	OCT_offer_usa_investment = {
		icon = GFX_goal_generic_construct_civ_factory
		allowed = {
			original_tag = OCT
			has_country_flag = OCT_can_offer_new_world_deals
			USA = { exists = yes }
		}
		visible = { USA = { exists = yes } }
		available = {
			is_subject = no
			NOT = { has_war_with = USA }
			USA = {
				is_subject = no
				NOT = { has_war_with = OCT }
			}
		}
		cost = 25
		fire_only_once = yes

		complete_effect = {
			USA = { country_event = { id = OCT_new_world_deal.1 days = 1 } }
		}
	}

	OCT_offer_bra_investment = {
		icon = GFX_goal_generic_construct_civ_factory
		allowed = {
			original_tag = OCT
			has_country_flag = OCT_can_offer_new_world_deals
			BRA = { exists = yes }
		}
		visible = { BRA = { exists = yes } }
		available = {
			is_subject = no
			NOT = { has_war_with = BRA }
			BRA = {
				is_subject = no
				NOT = { has_war_with = OCT }
			}
		}
		cost = 25
		fire_only_once = yes

		complete_effect = {
			BRA = { country_event = { id = OCT_new_world_deal.2 days = 1 } }
		}
	}
}